# 白名單式：預設忽略一切，只放行剪輯知識庫與說明文件。
#
# 這樣寫是刻意的 —— 這個資料夾裡有 ElevenLabs 金鑰、受訪者影片原檔，
# 工具腳本裡還留著上一支片的受訪者姓名，而 AI_Cut 是公開 repo。
# 黑名單漏一條就是不可逆的外洩，白名單漏一條只是少備份一個檔案。
#
# 要新增備份對象時，在下面補一條 `!` 規則，不要改成黑名單。

/*
!/.gitignore
!/CLAUDE.md
!/README.md

# 知識庫本體：敘事規範與視覺規範
!/skill/
/skill/*
!/skill/interview-video-story-editor/

# 流程防線與自動備份。只放行這兩項 ——
# settings.local.json 是個人覆寫，其餘是快取，都不該進 repo。
!/.claude/
/.claude/*
!/.claude/settings.json
!/.claude/hooks/

# 生成腳本。這三支寫著上一支片的受訪者姓名與年級，
# 每個新專案本來就要改寫，不進公開 repo。
!/工具/
/工具/make_cards.py
/工具/make_broll.py
/工具/make_ae_cards.jsx

# 品牌素材：字型是 fontTools 產的靜態字重，重做麻煩所以帶著走。
# 影片原檔只保留說明檔，讓 clone 下來目錄還在。
!/素材/
/素材/*
!/素材/品牌素材/
!/素材/影片原檔/
/素材/影片原檔/*
!/素材/影片原檔/放這裡.md

# 空目錄：git 不追蹤資料夾本身，靠 .gitkeep 佔位
!/工作區/
/工作區/*
!/工作區/.gitkeep

# 明確排除，即使有人日後放寬上面的規則
**/.env
**/.DS_Store
**/__pycache__/
