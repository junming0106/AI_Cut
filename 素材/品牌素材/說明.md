# 品牌素材

跨專案共用，通常不必更動。

| 檔案 | 用途 |
|---|---|
| `*.ttf` | 字型。會被複製進交付包，收件人要自行安裝 |
| Logo（png/webp/jpg 皆可） | 片尾卡。給 AE 用時會自動轉成 PNG |

## 換品牌時

換掉 Logo 與字型即可。若字型是可變字型（Variable Font），
要先產生各字重的靜態實例 —— 可變字型的預設實例常是 Thin，
AE 與 Premiere 直接指定會變細體。
